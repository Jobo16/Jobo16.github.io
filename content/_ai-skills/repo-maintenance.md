---
title: "内容仓库维护规则"
draft: true
---

# 内容仓库维护规则

## 规则

1. 新文章按类型放入对应文件夹。
2. 图片统一放入 `assets/`。
3. 不允许出现重复文件名导致 URL 混乱。
4. 不允许删除已发布文章，除非用户明确要求。
5. 修改标题时不要自动改文件名。
6. 大规模整理前，先列出移动和重命名计划。
7. 发布前检查内部链接是否有效。
8. 发布前检查是否有 `draft: false` 但缺少 `title` 或 `description` 的文件。
